/*
    [Intro and What is Regular Expression] L134
    => A regular expression, often abbreviated as (regex) or (regexp).
    => is a pattern-matching mechanism available in JavaScript (JS)
    and many other programming languages.
    It allows you to search, match, and manipulate strings based on specific patterns.

    =>  you can create a regular expression using the RegExp object or,
    by using the literal syntax with forward slashes (/pattern/)
*/
var regex = new RegExp("Hello World!");
console.log(regex);
